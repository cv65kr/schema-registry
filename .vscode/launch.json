{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Schema registry",
            "type": "go",
			"request": "launch",
			"mode": "debug",
            "program": "${workspaceFolder}/cmd/schema_registry/main.go"
        }
    ]
}